server:
  port: 8086

spring:
  application:
    name: api-gateway001

  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: ruta-usuarios
              uri: lb://ms-usuarios
              predicates:
                - Path=/api/usuarios/**
              filters:
                - StripPrefix=1

            - id: ruta-notas-metas
              uri: lb://ms-notas-metas
              predicates:
                - Path=/api/metas/**
              filters:
                - StripPrefix=1

eureka:
  client:
    enabled: true
    service-url:
      defaultZone: ${EUREKA_URL:http://eureka-server:8761/eureka}